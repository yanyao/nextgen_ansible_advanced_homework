---
# tasks file for roles/osp-instance-delete
  - name: Fetch Instance Info
    os_server_info:
      cloud: "openstack" 
    register: servers
  - name: Delete instances
    os_server:
      cloud: "openstack"
      state: absent
      name: "{{ item.openstack_servers.id }}"
    loop: servers 
   
